interface:
  display_name: "Claude Code Local"
  short_description: "Repo-specific patterns and workflows for claude-code-local"
  default_prompt: "Use the claude-code-local repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true